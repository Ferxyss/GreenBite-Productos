services:

  mongodb:
    image: mongo:8
    container_name: greenbite-mongo

    ports:
      - "27017:27017"

    environment:
      MONGO_INITDB_DATABASE: greenbite_productos

    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: